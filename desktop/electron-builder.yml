appId: io.github.josunlp.checkai.desktop
productName: CheckAI Desktop
artifactName: checkai-desktop-${version}-${os}-${arch}.${ext}
directories:
  output: release
files:
  - dist/**/*
  - dist-electron/**/*
mac:
  category: public.app-category.developer-tools
linux:
  target:
    - AppImage
    - tar.gz
win:
  target:
    - nsis
